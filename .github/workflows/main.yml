name: Playground

on:
  workflow_dispatch:

jobs:
  playground:
    runs-on: ubuntu-latest
  
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Test
        run: |
          echo "Hello world"
